{
  "name": "shared-workflows-playwright-smoke",
  "version": "1.0.0",
  "description": "Minimal Playwright smoke test used to validate the shared playwright.yml reusable workflow",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1"
  }
}
